[English](USER-GUIDE.md) | 中文

# 乐北斗

在本文件所在目录打开命令行（在文件夹地址栏输入 `powershell` 回车即可），运行：

```powershell
.\runtime\node\node.exe .\app\launch.cjs
```

无需安装 Node、Python 或下载依赖。启动后自动打开默认浏览器，命令行窗口随即可以关闭——服务在后台继续运行；以后重复运行这条命令只会打开已经开着的那个程序。请将整个文件夹解压到可写位置，勿直接从压缩包内运行。（用 cmd 而不是 PowerShell 的话，去掉路径前面的 `.\`。）

停止程序：在同一目录运行下面这条命令。关闭浏览器不会停止后台服务；识别期间请先停止识别或等它完成。

```powershell
.\runtime\node\node.exe .\app\server\server.mjs --home . --stop
```

## 第一次使用

打开后是空曲库，点击“新建歌曲”上传乐谱照片即可开始。识别需要先在“设置”里填好模型密钥并验证；密钥由 Windows 当前用户加密后保存在本机，不会随曲库一起备份。

## 数据与备份

所有歌曲数据都在本目录 `data` 中。`library.json` 是曲目索引；`songs/<ID>/song.json` 是当前歌曲，`images` 是原图及识别图片，`recognition` 是识别原始记录，`draft.json` 是未提交草稿。`settings.json` 保存设置，`practice.json` 保存练习位置。

清除浏览器缓存、换浏览器或改变端口不会清除这些文件；按上面的启动命令重新打开即可。修改失败时界面显示“未保存”；出现其他窗口已更新的提示时，先保留当前编辑内容，再刷新，避免覆盖新版本。

迁移到其他电脑：先停止程序，再复制整个乐北斗目录。密钥由 Windows 当前用户加密，换电脑或用户后需在设置中重新填写。密钥单独存于 `private/credentials`，不要把此目录发给别人。

备份：停止程序，把整个 `data` 文件夹复制到安全位置。恢复：停止程序，先另存当前 `data`，再用完整备份替换它。不要混合不同备份中的单个文件。`data/backups` 保存每次修改前的相关文件，供故障排查；它不是完整曲库备份。未完成事务会在下次启动时自动恢复。

## 手机端

手机安装 `Android/MusicVector.apk`。已安装且通过 USB 连接并允许调试的 Android 手机，会随启动命令一起打开乐北斗；不会自动安装或覆盖手机应用。未连接手机不影响电脑使用。

电脑点击“发送到手机”，手机端扫描二维码。手机和电脑应连接同一 Wi-Fi；如 Windows 防火墙询问，请仅允许可信的专用网络。二维码十分钟内有效，关闭发送面板会释放；仅此临时曲目可被取走，局域网无法访问整个曲库或密钥。端口冲突时程序自动换端口，请使用本次生成的二维码。

## 识别和播放

识别仍需联网调用模型，并在设置中验证自己的 API 密钥。几何分析在本机完成，识别结果和过程记录保存到本地；失败不会自动重试。意外关闭后保留已保存部分并标记中断。

保留桌面与手机最新的时值播放调整：发声包络收束在音符实际时值内；手机单音试听保留完整采样。编辑界面支持行内变拍、连接专属工具区，以及选中弧线起点后按 `W` 直接删除。

运行日志在 `data/logs/startup.log`。当前只提供 Windows x64 便携包；手机端为安卓 8.0 及以上，安装包在 `Android` 目录。
