# 正式签名配置示例。
#
# 复制成 keystore.properties（该文件已被忽略，不会进仓库）并填好四项，
# 然后 `gradle :app:assembleRelease` 就会用这把密钥签名。
# 生成密钥库（在仓库之外保存，至少备份两处）：
#
#   keytool -genkeypair -v -keystore E:\keys\musicvector.jks -alias musicvector \
#     -keyalg RSA -keysize 4096 -validity 10000
#
# 注意：密钥库和口令一旦丢失，就无法再给已经安装过旧版本的用户升级。
#      更换签名会让所有用户必须卸载重装。

storeFile=E:\\keys\\musicvector.jks
storePassword=改成你的密钥库口令
keyAlias=musicvector
keyPassword=改成你的密钥口令
